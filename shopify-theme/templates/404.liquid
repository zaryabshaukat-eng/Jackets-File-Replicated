<div class="not-found-page">
  <div class="container">
    <div class="not-found-page__inner">
      <span class="not-found-page__code">404</span>
      <h1 class="not-found-page__title">Page Not Found</h1>
      <p class="not-found-page__desc">The page you're looking for has moved, or no longer exists. Let's get you back on track.</p>
      <div class="not-found-page__actions">
        <a href="{{ shop.url }}" class="btn btn-primary">Go Home</a>
        <a href="{{ shop.url }}/collections/all" class="btn btn-outline">Browse Products</a>
      </div>
    </div>
  </div>
</div>

<style>
.not-found-page { min-height: 70vh; display: flex; align-items: center; padding: 80px 0; }
.not-found-page__inner { text-align: center; max-width: 500px; margin: 0 auto; }
.not-found-page__code { font-family: var(--font-serif); font-size: 120px; font-weight: 300; color: var(--clr-light-border); line-height: 1; display: block; margin-bottom: 16px; }
.not-found-page__title { font-family: var(--font-serif); font-size: 36px; font-weight: 300; margin-bottom: 16px; }
.not-found-page__desc { color: var(--clr-mid); font-size: 16px; line-height: 1.7; margin-bottom: 36px; }
.not-found-page__actions { display: flex; gap: 14px; justify-content: center; flex-wrap: wrap; }
</style>
